#Basic use of dictionaries

def main():
  spacecraft = {"name": "Rogers", "distance": 500}
  print(create_report(spacecraft))

def create_report(spacecraft):
  return f"""
  ======== REPORT =========

  Name: {spacecraft["name"]}
  Distance: {spacecraft["distance"]} AU

  =========================
  """
##################################
#what can go wrong? not calling distance

def main():
  spacecraft = {"name": "Telescope", "distance": 500}
#another way of adding to dictionary
  spacecraft["distance"] = 0.01
  print(create_report(spacecraft))

def create_report(spacecraft):
  return f"""
  ======== REPORT =========

  Name: {spacecraft["name"]}
  Distance: {spacecraft["distance"]} AU

  =========================


##################################
#should return 'unknown' since distance is not defined.

def main():
  spacecraft = {"name": "Telescope", "distance": 500}
  print(create_report(spacecraft))

def create_report(spacecraft):
  return f"""
  ======== REPORT =========

  Name: {spacecraft.get("name", "Unknown")}
  Distance: {spacecraft.get("distance", "Unknown)} AU

  =========================

##################################
#update dictionary

def main():
  spacecraft = {"name": "Telescope", "distance": 500}
  spacecraft.update({"distance": 0.01, "orbit": "Sun"})
  print(create_report(spacecraft))

def create_report(spacecraft):
  return f"""
  ======== REPORT =========

  Name: {spacecraft.get("name", "Unknown")}
  Distance: {spacecraft.get("distance", "Unknown)} AU
  Orbit: {spacecraft.get("orbit, "Unknown")}

  =========================

##################################
#new dictionary

distances = {
  "Voyager 1": 163
  "Voyager 2": 136
  "Pioneer 10": 80
  "New Horizons": 58
  "Pioneer 11": 44
}

def main():
  for name in distances.keys():
    print(f"{name} is {distances[name] AU from Earth"})
  for distance in distances.values():
    print(f"{distance} AU is {convert(distance)} m")

def convert(au)
  return au * 149597870700

main()
  
