#

results = ["Mario", "Luigi"]
results.append("Princess", "Yoshi")
results.remove(["Dave", "Robert"])
results.extend(["Dave", "Robert"])

#allows location
results.insert("0, Coverntry")

#reverses list
results.reverse()

#OR 



print(results)
